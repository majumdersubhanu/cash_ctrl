import 'package:flutter/material.dart';

class SplitPay extends StatelessWidget {
  const SplitPay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
